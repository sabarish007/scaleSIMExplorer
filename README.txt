usage: explorer.py [-h] [-rt RUN_TYPE] [-ht ARRAY_HEIGHT] [-wt ARRAY_WIDTH]
                   [-if IFMAP_SZ] [-fs FILTER_SZ] [-of OFMAP_SZ]
                   [-cfg CFG_FILE] [-out OUT_FILE]

Explore ScaleSIM design choices!

optional arguments:
  -h, --help            show this help message and exit
  -rt RUN_TYPE, --run_type RUN_TYPE
  -ht ARRAY_HEIGHT, --array_height ARRAY_HEIGHT
  -wt ARRAY_WIDTH, --array_width ARRAY_WIDTH
  -if IFMAP_SZ, --ifmap_sz IFMAP_SZ
  -fs FILTER_SZ, --filter_sz FILTER_SZ
  -of OFMAP_SZ, --ofmap_sz OFMAP_SZ
  -cfg CFG_FILE, --cfg_file CFG_FILE
  -out OUT_FILE, --out_file OUT_FILE
